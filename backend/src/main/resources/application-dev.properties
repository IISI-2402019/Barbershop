# ==========================================
# Local Development Configuration (dev profile)
# ==========================================

# 1. Database Configuration
# ------------------------------------------
# Option A: Use H2 In-Memory Database (Default for quick start)
# spring.datasource.url=jdbc:h2:mem:barbershopdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Option B: Use Local PostgreSQL (Uncomment and fill in if you have local Postgres)
# Connected to Railway DB via Proxy for local dev
spring.datasource.url=jdbc:postgresql://switchyard.proxy.rlwy.net:59175/railway
spring.datasource.username=postgres
spring.datasource.password=YRYFvtaIMeJQrckzqOOPfNwerJnGlmvk
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# 2. JPA / Hibernate Settings
# ------------------------------------------
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.h2.console.enabled=true

# 3. Server Port
# ------------------------------------------
server.port=8080
