# ==========================================
# Production Configuration (prod profile)
# ==========================================

# 1. Database Configuration (Railway)
# ------------------------------------------
# Railway automatically injects DATABASE_URL, but we need to parse it.
# The parsing logic is handled in RailwayDataSourceConfig.java
# This file serves as a placeholder for other prod-specific settings.

spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# 2. Security / CORS (If needed)
# ------------------------------------------
# cors.allowed.origins=https://your-frontend-domain.com
